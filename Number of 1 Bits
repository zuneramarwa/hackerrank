class Solution:
    def hammingWeight(self, n: int) -> int:
        k = n
        answer = 0
        while k != 0:
            answer = answer + (k&1)
            k = k>>1
        return answer
        
